# FenrirOS - unpackfs module config
# /etc/calamares/modules/unpackfs.conf
#
# Tells Calamares where to find the squashfs root filesystem
# and where to unpack it on the target system.

---
unpack:
    - source: "/run/archiso/airootfs"
      sourcefs: "squashfs"
      destination: ""
