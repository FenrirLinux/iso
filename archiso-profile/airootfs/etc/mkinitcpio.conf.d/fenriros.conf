# FenrirOS mkinitcpio config for live ISO
# Ensures CD-ROM and storage modules are available at boot

MODULES=(ahci sr_mod iso9660 squashfs loop overlay virtio virtio_pci virtio_blk)
BINARIES=()
FILES=()
HOOKS=(base eudev microcode autodetect modconf kms keyboard keymap consolefont block filesystems fsck archiso)
